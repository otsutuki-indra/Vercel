name: Deploy to AeonFree
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftpupload.net
          username: mseet_41609007
          password: bdr2krafi2009
          local-dir: ./
          server-dir: public_html/
          protocol: ftp